# Minimal project config — currently just pytest settings.
[tool.pytest.ini_options]
# Add the project root to sys.path so tests can `from src import ...`.
pythonpath = ["."]
testpaths = ["tests"]
markers = [
    "slow: reads the full 45MB raw dataset (deselect with -m 'not slow')",
]

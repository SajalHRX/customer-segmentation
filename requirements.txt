# Requires Python >= 3.12  (PyMC-Marketing needs >=3.10; project standardised on 3.12)

# Core data stack
pandas>=2.0
numpy>=1.26
openpyxl>=3.1          # read the Online Retail II .xlsx

# Modelling & validation
scikit-learn>=1.4      # K-Means, GMM, scaling, silhouette
scipy>=1.11            # hierarchical clustering, stats
pymc-marketing>=0.19   # Bayesian BG/NBD + Gamma-Gamma CLV (successor to lifetimes)
arviz>=0.18            # posterior diagnostics & predictive checks

# Visualisation
matplotlib>=3.8
seaborn>=0.13

# Notebook environment
jupyter>=1.0
notebook>=7.0

# Testing
pytest>=8.0
